import express from "express";

const router= express.Router();


router.get("/settings", );
router.patch("/settings/:planUuid", );

// Controller Responsibilities
// Maintenance mode
// Feature flags
// Global tax rates
// Currency
// Email templates
// Branding

export default router;
